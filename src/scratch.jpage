import edu.cnm.deepdive.Canis;

Canis rex = new edu.cnm.deepdive.CanisLupusFamiliaris(false);

rex.vocalize();// called by CanisLupuFamiliaris
rex.hunt(); // called by CanisLupus
rex.about(); // static class invokation depends only on the reference type.  The
System.out.println(rex);